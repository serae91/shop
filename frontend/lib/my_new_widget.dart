import 'package:flutter/material.dart';

class MyNewWidget extends StatelessWidget {
  const MyNewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hallo aus MyWidget!"),
    );
  }
}